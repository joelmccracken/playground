#lang scheme
; ill just answer this as a comment.
; queen-cols gets executed n times per level k.
; I don't know the exact runtime, but I would 
; estimate it at something along the lines of
; O(T!)

; this is a lazy answer, I know, but runtimes bore me