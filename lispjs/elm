import Html exposing (text)

type Expression
  = Begin (List Expression)
  | Fn (List Symbol) (List Expression)
  | List Expression
  | Call Expression (List Expression)

type Symbol
  = Sym String

main =
  text <| toString <| Begin [
  Call (Fn [Sym "x"] [])
       []

  ]